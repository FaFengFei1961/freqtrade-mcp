{
  "_comment": "Merge this `mcpServers.freqtrade` entry into your existing ~/.gemini/settings.json (preserve other servers like context7, pencil, etc.). Adjust the absolute path.",
  "mcpServers": {
    "freqtrade": {
      "command": "/ABSOLUTE/PATH/TO/freqtrade-mcp/.venv/bin/freqtrade-mcp",
      "args": [],
      "env": {},
      "timeout": 60000
    }
  }
}
